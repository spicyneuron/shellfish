#!/usr/bin/env zsh

emulate -R zsh
setopt no_aliases no_multios pipe_fail

typeset -gr SF_ROOT=${0:A:h:h}
typeset -gr SF_SHARE=$SF_ROOT/share

case ${1-} in
  run|create|config|build-request|send-request)
    exec -- "$SF_ROOT/libexec/$1/main.zsh" "${@:2}"
    ;;
  -c|--continue|-r|--resume)
    exec -- "$SF_ROOT/libexec/resume/main.zsh" "$@"
    ;;
  -h|--help)
    exec cat -- "$SF_SHARE/help.txt"
    ;;
  --version)
    print -r -- "shellfish $(<"$SF_ROOT/VERSION")"
    ;;
  *) exec -- "$SF_ROOT/libexec/tui/main.zsh" "$@" ;;
esac
