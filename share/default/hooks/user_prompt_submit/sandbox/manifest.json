{
  "render": {"initial_user_text":"","user_text":"${name}\n${output.stdout}${output.stderr}","model_text":"${output.stdout}"},
  "match": {
    "pattern": "^/sandbox( [^\\n]*)?\\z"
  },
  "help": {
    "usage": "/sandbox [OP DIR]",
    "description": "List or update session sandbox grants"
  }
}
