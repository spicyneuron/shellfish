{
  "running": "user_shell · Running shell command…",
  "match": {
    "pattern": "^![^\\n]*\\z"
  },
  "help": {
    "usage": "!COMMAND",
    "description": "Run COMMAND and stage its output as context"
  }
}
