{
  "match": {
    "pattern": "^/server\\z"
  },
  "render": {
    "user_before": "",
    "user_after": "${script}\n${output.stdout}${output.stderr}",
    "model_after": "${output.stdout}"
  },
  "help": {
    "usage": "/server",
    "description": "Serve this session in a browser"
  }
}
