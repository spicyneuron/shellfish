{
  "description": "Load an available agent skill when its description matches the task. The tool returns the skill's complete instructions and its directory for resolving referenced resources.",
  "input_schema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["name"],
    "properties": {
      "name": {"type": "string", "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$", "maxLength": 64, "description": "Skill name from the available agent skills context"}
    }
  },
  "render": {
    "user_before": "${script} · ${input.name}",
    "user_after": "${script} · ${input.name}\n${output.stdout}${output.stderr}",
    "model_after": "${output.stdout}${output.stderr}"
  },
  "permission_preview": "${input.name}",
  "sandbox": false
}
